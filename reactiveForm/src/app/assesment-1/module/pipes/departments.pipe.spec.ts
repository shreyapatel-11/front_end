import { DepartmentsPipe } from './departments.pipe';

describe('DepartmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
